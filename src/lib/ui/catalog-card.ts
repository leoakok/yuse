/** Outer shell shared by catalog grid cards. */
export const catalogCardShellClassName =
  "group flex h-full flex-col overflow-hidden transition-colors hover:bg-muted/40";

/** Preview thumbnail area at the top of catalog cards (208px / h-52). */
export const catalogCardPreviewClassName =
  "relative block h-52 shrink-0 overflow-hidden border-b bg-muted/30";
